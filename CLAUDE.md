# CLAUDE.md

이 저장소에서 작업할 때 참고할 규칙입니다.

## 프로젝트 성격

한국어 Web3 학습 사이트. 독자는 **프론트엔드는 알지만 블록체인은 처음인 개발자**입니다.
이 독자 설정이 모든 판단의 기준입니다.

## 절대 규칙

1. **의존성을 함부로 추가하지 마세요.** 현재 `next`, `react`, `react-dom` 셋뿐입니다.
   Tailwind, MDX, UI 라이브러리 도입은 사용자에게 먼저 확인하세요.
   스타일은 `app/globals.css`의 CSS 변수 체계로 해결합니다.

2. **콘텐츠는 JSX로 옮기지 마세요.** `content/*.html`이 순수 HTML인 것은 의도된 설계입니다.
   코드 예제에 `{`, `${`, `class`, `<`가 그대로 들어가야 하기 때문입니다. MDX 전환도 같은 이유로 신중하게.

3. **사실을 지어내지 마세요.** 수치·날짜·버전은 전부 2026년 7월 기준으로 출처 확인된 값입니다.
   새로 추가하거나 갱신할 때는 반드시 웹에서 확인하고, 13장 참고자료에 출처를 추가하세요.

4. **`lib/chapters.ts`가 단일 출처입니다.** 챕터 순서·제목·slug는 여기서만 바꿉니다.
   사이드바, 검색, 이전/다음 네비게이션, `generateStaticParams`가 전부 이 배열을 읽습니다.

## 아키텍처 메모

- **서버/클라이언트 경계**: 페이지는 전부 서버 컴포넌트(SSG). 상호작용이 필요한 것만 `"use client"`.
  `ChapterBody`가 클라이언트인 이유는 코드 복사 버튼과 데모 주입 때문입니다.

- **데모 주입 방식**: `ChapterBody`가 `<div data-demo="이름"></div>` 마커로 HTML을 split 해서
  사이사이에 `Demos.tsx`의 컴포넌트를 끼워 넣습니다. 새 데모는 `DEMOS` 레지스트리에 등록하면 됩니다.

- **테마**: `html[data-theme]` 속성 + CSS 변수. `layout.tsx`의 인라인 스크립트가 hydration 전에
  적용해 깜빡임을 막습니다. 이 스크립트를 건드리면 FOUC가 생깁니다.

- **진도 저장**: `localStorage` + 커스텀 이벤트(`w3-progress-change`)로 사이드바와 동기화.
  서버 상태 없음. 로그인 기능을 넣는다면 이 부분을 교체하게 됩니다.

- **`content/*.html`은 원본이 아닙니다.** 최초에 단일 HTML 문서에서 분해해 만들었습니다.
  지금은 이 파일들이 원본이니 여기를 직접 수정하세요.

## 문체 가이드

- 존댓말, 담백하게. 과장·감탄사·이모지 남발 금지.
- 낯선 개념은 **프론트엔드 기존 지식에 매핑**해서 설명 (`.card.fe` 박스 활용).
  예: ABI ↔ OpenAPI, 인덱서 ↔ read replica, 서명 로그인 ↔ JWT.
- 장점만 쓰지 않습니다. 한계와 실패 사례를 같은 비중으로 씁니다.
- 코드 주석은 한국어. `⚠️`로 실무 함정을 표시.

## 자주 하는 작업

**챕터 추가**
```
1. content/{slug}.html 작성
2. lib/chapters.ts에 항목 추가
3. npm run build 로 정적 경로 생성 확인
```

**데모 추가**
```
1. components/Demos.tsx 에 컴포넌트 작성 + DEMOS에 등록
2. content 안에 <div data-demo="이름"></div> 삽입
```

**검증**
```
npm run build   # 타입 체크 + 19페이지 정적 생성이 전부 통과해야 함
```

## 남은 작업 아이디어

- [ ] OG 이미지 생성 (`app/opengraph-image.tsx`)
- [ ] 챕터 내 목차(TOC) — h2/h3를 파싱해 우측 고정 네비
- [ ] 검색을 본문 전문 검색으로 확장 (현재는 제목·설명·태그만)
- [ ] `sitemap.ts`, `robots.ts`
- [ ] 실습 4번: 실제 퍼블릭 RPC로 최신 블록·가스 조회 (`viem` 추가 필요 — 의존성 확인받을 것)
- [ ] 코드 블록 하이라이팅을 수동 `<span>`에서 Shiki로 교체 검토
